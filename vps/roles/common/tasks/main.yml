- name: Update apt
  apt:
    update_cache: yes

- name: Install base packages
  apt:
    name:
      - curl
      - git
      - ufw
      - python3
      - python3-pip
    state: present
